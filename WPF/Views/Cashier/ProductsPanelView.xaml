<UserControl x:Class="UI.Views.Cashier.ProductsPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:UI.Controls"
             xmlns:vwp="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"
             mc:Ignorable="d"
             d:DesignWidth="700" d:DesignHeight="900">

    <Border Background="{StaticResource Color.Surface.Default}"
            BorderBrush="{StaticResource Color.Border.Default}"
            BorderThickness="1"
            CornerRadius="{StaticResource Radius.Md}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource Color.Surface.Subtle}"
                    BorderBrush="{StaticResource Color.Border.Default}"
                    BorderThickness="0,0,1,0"
                    Padding="{StaticResource Spacing.Xs2}">
                <ScrollViewer x:Name="CategoriesScrollViewer"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Categories}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <RadioButton GroupName="CashierCategories"
                                             IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                             Style="{StaticResource POS.RadioButton.Chip}"
                                             HorizontalAlignment="Stretch"
                                             MinHeight="{StaticResource Size.TouchTarget.Min}"
                                             Margin="{StaticResource Spacing.Xs}"
                                             Command="{Binding DataContext.SelectCategoryCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                             CommandParameter="{Binding}"
                                             Checked="CategoryRadioButton_Checked">
                                    <TextBlock Text="{Binding Name}"
                                               FontSize="{StaticResource Font.Size.Base}"
                                               FontWeight="{StaticResource Font.Weight.SemiBold}"
                                               TextAlignment="Center"
                                               TextWrapping="Wrap"/>
                                </RadioButton>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="{StaticResource Color.Surface.Subtle}"
                        BorderBrush="{StaticResource Color.Border.Default}"
                        BorderThickness="0,0,0,1"
                        Padding="{StaticResource Spacing.Md}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="🔍"
                                   FontSize="{StaticResource Font.Size.Md}"
                                   VerticalAlignment="Center"
                                   Margin="{StaticResource Spacing.RowIconGap}"/>
                        <TextBox x:Name="SearchTextBox"
                                 Grid.Column="1"
                                  Style="{StaticResource POS.TextBox.SearchInline}"
                                  Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                </Border>

                <Border Grid.Row="1"
                        Background="{StaticResource Color.Surface.Subtle}"
                        BorderBrush="{StaticResource Color.Border.Default}"
                        BorderThickness="0,0,0,1"
                        Padding="{StaticResource Spacing.Md}"
                        Visibility="{Binding HasSubCategories, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0"
                                    Orientation="Horizontal"
                                    VerticalAlignment="Center"
                                    Margin="{StaticResource Spacing.RowSectionGap}">
                            <TextBlock Text="{Binding SelectedCategory.Name}"
                                       FontSize="{StaticResource Font.Size.Xs}"
                                       FontWeight="{StaticResource Font.Weight.Medium}"
                                       Foreground="{StaticResource Color.Brand.Dark}"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text=" >"
                                       FontSize="{StaticResource Font.Size.Xs}"
                                       Foreground="{StaticResource Color.Text.Secondary}"
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                        <ScrollViewer Grid.Column="1"
                                      HorizontalScrollBarVisibility="Auto"
                                      VerticalScrollBarVisibility="Disabled">
                            <ItemsControl ItemsSource="{Binding VisibleSubCategories}">
                                <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Name}"
                                        Style="{StaticResource POS.Button.Ghost}"
                                        MinHeight="{StaticResource Size.TouchTarget.Min}"
                                        Margin="{StaticResource Spacing.Xs}"
                                        Command="{Binding DataContext.SelectSubCategoryCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}"
                                        Click="SubCategoryButton_Click"/>
                            </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <Grid Grid.Row="2">
                    <ItemsControl x:Name="ProductsItemsControl"
                                  ItemsSource="{Binding ProductsView}"
                                  ScrollViewer.CanContentScroll="True"
                                  VirtualizingPanel.IsVirtualizing="True"
                                  VirtualizingPanel.VirtualizationMode="Recycling"
                                  VirtualizingPanel.ScrollUnit="Pixel">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <vwp:VirtualizingWrapPanel Orientation="Horizontal"
                                                           ItemSize="156,128"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.Template>
                            <ControlTemplate>
                                <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                                              VerticalScrollBarVisibility="Auto"
                                              Padding="{StaticResource Spacing.Md}"
                                              CanContentScroll="True">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </ControlTemplate>
                        </ItemsControl.Template>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                    <Button Style="{StaticResource POS.Button.ProductCard}"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        MinHeight="{StaticResource Size.TouchTarget.Min}"
                                        Command="{Binding DataContext.AddProductCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}">
                                    <Grid Margin="{StaticResource Spacing.3}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0"
                                                   Text="{Binding DisplayName}"
                                                   FontSize="{StaticResource Font.Size.Xs}"
                                                   FontWeight="{StaticResource Font.Weight.SemiBold}"
                                                   TextWrapping="Wrap"
                                                   TextAlignment="Center"
                                                   VerticalAlignment="Top"/>
                                        <controls:CurrencyText Grid.Row="1"
                                                               Amount="{Binding UnitPrice}"
                                                               AmountForeground="{StaticResource Color.Brand.Dark}"
                                                               FontSize="{StaticResource Font.Size.Xs}"
                                                               FontWeight="{StaticResource Font.Weight.Bold}"
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Bottom"/>
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Border Background="{StaticResource Color.Surface.Warning}"
                            BorderBrush="{StaticResource Color.Border.Warning}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource Radius.Lg}"
                            Padding="{StaticResource Spacing.Lg}"
                            MaxWidth="280"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="{Binding ShowNoProductsMessage, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="⌕"
                                       FontSize="{StaticResource Font.Size.2Xl}"
                                       Foreground="{StaticResource Color.Text.Warning}"
                                       HorizontalAlignment="Center"
                                       Margin="{StaticResource Spacing.DialogTitleGap}"/>
                            <TextBlock Text="{DynamicResource Cashier.NoProductsTitle}"
                                       FontSize="{StaticResource Font.Size.Md}"
                                       FontWeight="{StaticResource Font.Weight.Bold}"
                                       Foreground="{StaticResource Color.Text.Warning}"
                                       TextAlignment="Center"/>
                            <TextBlock Text="{DynamicResource Cashier.NoProductsSub}"
                                       FontSize="{StaticResource Font.Size.Caption}"
                                       Foreground="{StaticResource Color.Text.Warning}"
                                       TextAlignment="Center"
                                       TextWrapping="Wrap"
                                       Margin="{StaticResource Spacing.DialogTitleGap}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</UserControl>

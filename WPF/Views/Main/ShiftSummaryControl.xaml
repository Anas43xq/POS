<!--
    ╔══════════════════════════════════════════════════════╗
    ║   Shift Summary list. Cashier = left, Cash Diff.       ║
    ║   = right (numeric exception). Kept in the main        ║
    ║   dashboard row — never pushed to a footer.             ║
    ╚══════════════════════════════════════════════════════╝
-->
<UserControl x:Class="UI.Views.ShiftSummaryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="360" d:DesignWidth="360">

    <Border Style="{StaticResource CardBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource POS.Manager.HomeCardHeader}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Style="{StaticResource POS.Manager.HomeCardTitle}">
                        <Run Text="👥  "/>
                        <Run Text="{DynamicResource Manager.Home.ShiftSummary}"/>
                    </TextBlock>
                    <Button Grid.Column="1"
                            Style="{StaticResource POS.Button.HeaderAction}"
                            Content="{DynamicResource Manager.Home.ViewAll}"
                            Command="{Binding OpenShiftManagementCommand}"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="{StaticResource Spacing.3}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="70"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{DynamicResource Manager.Home.ColCashier}" Style="{StaticResource BadgeText}"/>
                <TextBlock Grid.Column="1" Text="{DynamicResource Manager.Home.ColCashDifference}"
                           Style="{StaticResource BadgeText}"
                           TextAlignment="Right"/>
            </Grid>
             <Border Grid.Row="1" BorderBrush="{StaticResource Color.Border.Default}" BorderThickness="{StaticResource Thickness.BottomHairline}"
                    Margin="{StaticResource Spacing.3}" VerticalAlignment="Top"/>

            <ScrollViewer Grid.Row="2" Padding="{StaticResource Spacing.3}" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding ShiftSummaries}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource ListRow}" Padding="{StaticResource Spacing.1}">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="70"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding CashierName}" Style="{StaticResource POS.Manager.HomeListPrimaryText}"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding CashDifference, StringFormat={}{0:+0.00;-0.00}}"
                                                   Style="{StaticResource POS.Manager.HomeCashDifferenceText}"/>
                                    </Grid>
                                    <TextBlock Style="{StaticResource POS.Manager.HomeListMetaText}" Margin="{StaticResource Spacing.Xxs}">
                                        <Run Text="{DynamicResource Common.Open}"/><Run Text=" "/>
                                        <Run Text="{Binding OpenedAt, Mode=OneWay, StringFormat=HH:mm}"/>
                                        <Run Text="  ·  "/><Run Text="{DynamicResource Common.Close}"/><Run Text=" "/>
                                        <Run Text="{Binding ClosedAtDisplay, Mode=OneWay}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>

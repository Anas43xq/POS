<Window x:Class="UI.Views.Modifiers.ModifierOptionEditDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="{Binding OptionDialogTitle}"
        Width="{StaticResource Size.Dialog.ModifierOption.Width}"
        Height="{StaticResource Size.Dialog.ModifierOption.Height}"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Style="{StaticResource POS.Window.Overlay}"
        KeyDown="ModifierOptionEditDialogView_KeyDown">

    <Grid x:Name="OverlayGrid"
          Style="{StaticResource POS.Dialog.Overlay}"
          MouseDown="OverlayGrid_MouseDown">
        <Border x:Name="DialogCard"
                Style="{StaticResource POS.Dialog.Card}"
                Width="{StaticResource Size.Dialog.ModifierOption.Width}"
                MaxHeight="{StaticResource Size.Dialog.ModifierOption.Height}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Margin="{StaticResource Spacing.DialogOuter}"
                MouseDown="DialogCard_MouseDown">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Style="{StaticResource POS.Dialog.Header}"
                        Padding="{StaticResource Spacing.DialogHeader}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal">
                            <Border Style="{StaticResource POS.Dialog.IconBadge}"
                                    Margin="{StaticResource Spacing.RowIconGap}">
                                <Image Source="pack://application:,,,/UI;component/Assets/nav/modifiers.png"
                                       Style="{StaticResource POS.Manager.CategoryCardImage}"/>
                            </Border>
                            <TextBlock Text="{Binding OptionDialogTitle}"
                                       Style="{StaticResource POS.Dialog.Title}"/>
                        </StackPanel>

                        <Button Grid.Column="1"
                                Style="{StaticResource POS.Dialog.CloseButton}"
                                Content="✕"
                                Command="{Binding CancelOptionEditCommand}"/>
                    </Grid>
                </Border>

                <Border Grid.Row="1"
                        Style="{StaticResource POS.Dialog.NoticeDanger}"
                        Margin="{StaticResource Spacing.DialogErrorBannerMargin}"
                        Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="{Binding ErrorMessage}"
                               Style="{StaticResource POS.Dialog.ErrorText}"
                               TextWrapping="Wrap"/>
                </Border>

                <ScrollViewer Grid.Row="2"
                              VerticalScrollBarVisibility="Auto"
                              Padding="{StaticResource Spacing.3}">
                    <StackPanel>
                        <Grid Margin="{StaticResource Spacing.DialogFieldGap}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Row="0"
                                        Grid.ColumnSpan="2"
                                        Margin="{StaticResource Spacing.DialogFieldGap}">
                                <TextBlock Text="{DynamicResource Common.Name}"
                                           Style="{StaticResource POS.Dialog.Label}"
                                           Margin="{StaticResource Spacing.DialogLabelGap}"/>
                                <TextBox x:Name="NameBox"
                                         Text="{Binding EditOptionName, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource POS.TextBox.Default}"
                                         IsEnabled="{Binding IsNotBusy}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1"
                                        Grid.Column="0"
                                        Margin="{StaticResource Spacing.RowIconGap}">
                                <TextBlock Text="{DynamicResource Manager.ModifierOption.PriceAdd}"
                                           Style="{StaticResource POS.Dialog.Label}"
                                           Margin="{StaticResource Spacing.DialogLabelGap}"/>
                                <TextBox Text="{Binding EditOptionPrice, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource POS.TextBox.Default}"
                                         IsEnabled="{Binding IsNotBusy}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1"
                                        Grid.Column="1">
                                <TextBlock Text="{DynamicResource Manager.ModifierOption.DisplayOrder}"
                                           Style="{StaticResource POS.Dialog.Label}"
                                           Margin="{StaticResource Spacing.DialogLabelGap}"/>
                                <TextBox Text="{Binding EditOptionSortOrder, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource POS.TextBox.Default}"
                                         IsEnabled="{Binding IsNotBusy}"/>
                            </StackPanel>
                        </Grid>

                        <Border Style="{StaticResource POS.Dialog.InputBorder}"
                                Padding="{StaticResource Spacing.2}">
                            <StackPanel Orientation="Horizontal">
                                <CheckBox Content="{DynamicResource Manager.ModifierOption.AllowQuantity}"
                                          IsChecked="{Binding EditOptionAllowQuantity}"
                                          Style="{StaticResource POS.CheckBox.Default}"
                                          IsEnabled="{Binding IsNotBusy}"
                                          Margin="{StaticResource Spacing.RowIconGap}"/>

                                <CheckBox Content="{DynamicResource Manager.ModifierOption.DefaultRegular}"
                                          IsChecked="{Binding EditOptionIsDefault}"
                                          Style="{StaticResource POS.CheckBox.Default}"
                                          IsEnabled="{Binding IsNotBusy}"
                                          Margin="{StaticResource Spacing.RowIconGap}"/>

                                <CheckBox Content="{DynamicResource Manager.ModifierOption.Active}"
                                          IsChecked="{Binding EditOptionIsActive}"
                                          Style="{StaticResource POS.CheckBox.Default}"
                                          IsEnabled="{Binding IsNotBusy}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Row="3"
                        Style="{StaticResource POS.Dialog.Footer}"
                        Padding="{StaticResource Spacing.DialogFooter}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0"
                                Style="{StaticResource POS.Button.Secondary}"
                                Content="{DynamicResource Manager.ModifierOption.TranslationsButton}"
                                Command="{Binding OptionTranslationsCommand}"
                                HorizontalAlignment="Left"
                                IsEnabled="{Binding IsNotBusy}"/>

                        <StackPanel Grid.Column="1"
                                    Orientation="Horizontal">
                            <Button Style="{StaticResource POS.Button.Secondary}"
                                    Content="{DynamicResource Common.Cancel}"
                                    Command="{Binding CancelOptionEditCommand}"
                                    Margin="{StaticResource Spacing.DialogFooterCancelGap}"
                                    IsEnabled="{Binding IsNotBusy}"/>
                            <Button Style="{StaticResource POS.Button.Primary}"
                                    Content="{DynamicResource Common.Save}"
                                    Command="{Binding SaveOptionCommand}"
                                    IsEnabled="{Binding IsNotBusy}"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>

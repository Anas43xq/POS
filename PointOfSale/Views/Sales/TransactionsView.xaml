<!--
    ╔══════════════════════════════════════════════════════╗
    ║   Cafeteria Hawa POS  –  Manager · Transactions      ║
    ║   Filter bar + grouped DataGrid + pagination         ║
    ╚══════════════════════════════════════════════════════╝
-->
<UserControl x:Class="UI.Views.TransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:UI.Controls"
             xmlns:views="clr-namespace:UI.Views"
             mc:Ignorable="d"
             d:DesignWidth="1420" d:DesignHeight="820">

    <UserControl.Resources>

        <!-- Payment badge border (used inside DataGrid cell template) -->
        <Style x:Key="PaymentBadgeBorderStyle" TargetType="Border" BasedOn="{StaticResource BadgeBase}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding PaymentMethod}" Value="Cash">
                    <Setter Property="Background" Value="#DCFCE7"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding PaymentMethod}" Value="Card">
                    <Setter Property="Background" Value="#DBEAFE"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Payment badge text -->
        <Style x:Key="PaymentBadgeTextStyle" TargetType="TextBlock" BasedOn="{StaticResource BadgeText}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding PaymentMethod}" Value="Cash">
                    <Setter Property="Foreground" Value="#16A34A"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding PaymentMethod}" Value="Card">
                    <Setter Property="Foreground" Value="#1A6FC4"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Status badge border -->
        <Style x:Key="StatusBadgeBorderStyle" TargetType="Border" BasedOn="{StaticResource BadgeBase}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Completed">
                    <Setter Property="Background" Value="#DBEAFE"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Voided">
                    <Setter Property="Background" Value="{StaticResource DangerLight}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Pending">
                    <Setter Property="Background" Value="#FEF3C7"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Status badge text -->
        <Style x:Key="StatusBadgeTextStyle" TargetType="TextBlock" BasedOn="{StaticResource BadgeText}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Completed">
                    <Setter Property="Foreground" Value="#1A6FC4"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Voided">
                    <Setter Property="Foreground" Value="{StaticResource DangerDark}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Pending">
                    <Setter Property="Foreground" Value="#92400E"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <Grid Background="#EAF4FF">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Filter bar -->
            <RowDefinition Height="*"/>
            <!-- Table card -->
        </Grid.RowDefinitions>

        <!-- ══════════════════════════════════════
             FILTER BAR
             ══════════════════════════════════════ -->
        <views:TransactionsFilterPanelControl Grid.Row="0"/>

        <!-- ══════════════════════════════════════
             TRANSACTIONS TABLE CARD
             ══════════════════════════════════════ -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- header  -->
                    <RowDefinition Height="*"/>
                    <!-- grid    -->
                    <RowDefinition Height="Auto"/>
                    <!-- pager   -->
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource CardHeaderBorder}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{DynamicResource Manager.Transactions.TransactionsCardTitle}" Style="{StaticResource CardTitle}"/>
                        <!-- Busy spinner -->
                        <TextBlock Grid.Column="1"
                                   Text="{DynamicResource Manager.Transactions.Loading}"
                                   FontSize="11"
                                   Foreground="{StaticResource TextMuted}"
                                   VerticalAlignment="Center"
                                   Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </Grid>
                </Border>

                <!-- Transactions DataGrid -->
                <DataGrid x:Name="TransactionsGrid"
                          Grid.Row="1"
                          Style="{StaticResource ManagerDataGrid}"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          ItemsSource="{Binding TransactionsView}"
                          SelectedItem="{Binding SelectedTransaction, Mode=TwoWay}">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="{DynamicResource Manager.Transactions.ColReceipt}" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding ReceiptNumber}"
                                            FontSize="11"
                                            Cursor="Hand"
                                            HorizontalAlignment="Left"
                                            Background="#DBEEFF"
                                            Foreground="{StaticResource BrandDark}"
                                            BorderThickness="0"
                                            Padding="8,2"
                                            Command="{Binding DataContext.OpenReceiptCommand,
                                                              RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="Bg" Background="#DBEEFF" CornerRadius="4" Padding="8,2">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Bg" Property="Background" Value="#C4DDF5"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="{DynamicResource Manager.Transactions.ColAmount}" Width="120">
                            <DataGridTemplateColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ManagerDataGridColumnHeader}">
                                    <Setter Property="HorizontalContentAlignment" Value="Right"/>
                                </Style>
                            </DataGridTemplateColumn.HeaderStyle>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <controls:CurrencyText Amount="{Binding GrandTotal}"
                                                           Foreground="{StaticResource TextDark}"
                                                           HorizontalAlignment="Right"
                                                           VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="{DynamicResource Manager.Transactions.ColPayment}" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border HorizontalAlignment="Left" VerticalAlignment="Center"
                                            Style="{StaticResource PaymentBadgeBorderStyle}">
                                        <TextBlock Text="{Binding PaymentMethod}"
                                                   Style="{StaticResource PaymentBadgeTextStyle}"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="{DynamicResource Manager.Transactions.ColNotes}"
                                            Binding="{Binding Notes}" Width="*"/>
                        <DataGridTemplateColumn Header="{DynamicResource Manager.Transactions.ColStatus}" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border HorizontalAlignment="Left" VerticalAlignment="Center"
                                            Style="{StaticResource StatusBadgeBorderStyle}">
                                        <TextBlock Text="{Binding Status}"
                                                   Style="{StaticResource StatusBadgeTextStyle}"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="{DynamicResource Manager.Transactions.ColDateTime}"
                                            Binding="{Binding TransactionDate, StringFormat='yyyy-MM-dd HH:mm'}"
                                            Width="160"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Pagination footer -->
                <Border Grid.Row="2"
                        Background="{StaticResource CardHdrBg}"
                        BorderBrush="{StaticResource BrandBorder}"
                        BorderThickness="0,0.5,0,0"
                        Padding="14,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel VerticalAlignment="Center">
                            <TextBlock FontSize="11" Foreground="{StaticResource TextMuted}">
                                <Run Text="{DynamicResource Common.Page}"/>
                                <Run Text=" "/>
                                <Run Text="{Binding PageInfo, Mode=OneWay}"/>
                                <Run Text="  ·  "/>
                                <Run Text="{Binding TotalCount, Mode=OneWay}"/>
                                <Run Text=" "/>
                                <Run Text="{DynamicResource Manager.Transactions.PagerSuffix}"/>
                            </TextBlock>
                            <!-- Error banner -->
                            <TextBlock Text="{Binding ErrorMessage}"
                                       FontSize="11" Foreground="#A32D2D"
                                       Margin="0,4,0,0"
                                       TextWrapping="Wrap"
                                       Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Style="{StaticResource OutlineBtn}"
                                    Content="{DynamicResource Common.Previous}"
                                    Margin="0,0,6,0"
                                    Command="{Binding PreviousPageCommand}"/>
                            <Button Style="{StaticResource PrimaryGradientBtn}"
                                    Content="{DynamicResource Common.Next}"
                                    Command="{Binding NextPageCommand}"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>

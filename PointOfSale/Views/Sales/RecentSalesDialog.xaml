<Window x:Class="UI.Views.RecentSalesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:UI.Controls"
        mc:Ignorable="d"
        Title="Recent Sales"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">

    <Border Width="720"
            MaxHeight="620"
            Background="White"
            Padding="24"
            BorderBrush="#CBD5E1"
            BorderThickness="1"
            CornerRadius="8">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Recent Transactions" FontSize="22" FontWeight="Bold" Foreground="#0F172A" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="✕" FontSize="16" Padding="8" Background="Transparent" Command="{Binding CloseDialogCommand}"/>
            </Grid>

            <controls:RecentSalesList Grid.Row="1"
                                      Margin="0,0,0,18"
                                      ItemsSource="{Binding RecentSales}"
                                      OpenCommand="{Binding OpenReceiptCommand}"/>

            <!-- Footer -->
            <Grid Grid.Row="2">
                <TextBlock Text="Select Open to view the receipt again." Foreground="#64748B" FontSize="13" Margin="0,14,0,0"/>
            </Grid>
        </Grid>
    </Border>
</Window>
